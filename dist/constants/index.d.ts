export declare enum NODE_ENV {
    DEVELOPMENT = "development",
    TEST = "test",
    PRODUCTION = "production"
}
export declare enum OAUTH_GRANT_TYPE {
    PASSWORD = "password",
    REFRESH_TOKEN = "refresh_token"
}
export declare enum AUTH_STRATEGY {
    TOKEN = "token",
    REFRESH_TOKEN = "refresh_token"
}
export declare const DEFAULT_PAGE_NUMBER = 1;
export declare const DEFAULT_PAGE_SIZE = 20;
export declare enum DEFAULT_ACTIONS {
    FILTER = "filter",
    SHOW = "show",
    CREATE = "create",
    UPDATE = "update",
    DELETE = "delete"
}
